{
  "name": "santas-christmas-chase",
  "version": "1.0.0",
  "description": "A festive web-based game where Santa chases a robber while collecting presents",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "dependencies": {
    "phaser": "^3.55.2"
  },
  "devDependencies": {
    "vite": "^5.0.10",
    "firebase-tools": "^12.0.0"
  }
}
